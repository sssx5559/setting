if $t_Co > 2 || has("gui_running")
	syntax on
	set hlsearch
endif

set autoindent
set incsearch
set tabstop=4
set shiftwidth=4
set smartcase	"検索時に大文字を含んでいたら区別する
set number
set enc=utf-8
set fenc=utf-8
